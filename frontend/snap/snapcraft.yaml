name: vaulth_app
version: 1.0.0
summary: Generated by Icons Launcher
description: Generated by Icons Launcher package for Linux.

confinement: strict
base: core22
grade: stable

slots:
  dbus-vaulth_app: # adjust accordingly to your app name
    interface: dbus
    bus: session
    name: org.bar.vaulth_app # adjust accordingly to your app name

apps:
  vaulth_app:
    command: vaulth_app
    extensions: [gnome] # gnome includes the libraries required by flutter
    plugs:
    - network
    slots:
      - dbus-vaulth_app
parts:
  vaulth_app:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart # The main entry-point file of the application
